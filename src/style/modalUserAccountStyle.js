import { makeStyles } from "@material-ui/core/styles";

const modalUserAccountStyle = makeStyles({
    paper: {
        width: "auto",
        padding: 10,
    },
    title: {
        paddingTop: 10,
    },
    field: {
        marginTop: 20,
        marginBottom: 20,
        display: "block",
    },
});

export default modalUserAccountStyle;
